# Local variables

locals {
  # Example local variables
  # module_name = "example-module"
  
  # Example of merging default tags with user-provided tags
  # common_tags = {
  #   "Terraform"   = "true"
  #   "Module"      = local.module_name
  # }
  # tags = merge(local.common_tags, var.tags)
}
